# Phuket DACH Landing Page

Лендинг для инвестиций в недвижимость на Пхукете для DACH-аудитории.

## 🚀 Быстрый старт

```bash
npm install
npm run dev
# Открыть http://localhost:5173
```

## 📦 Деплой на GitHub Pages

1. Создайте репозиторий на GitHub
2. Обновите `base` в `vite.config.ts` на имя вашего репозитория
3. Запустите:

```bash
npm install
npm run build
npm run deploy
```

4. В Settings → Pages выберите branch: `gh-pages`

## 🔑 Админка

Доступна по адресу `/admin`

**Демо-вход:**
- Email: `admin@demo.com`
- Пароль: `demo123`

## 📁 Структура

- `/src/components` - UI компоненты
- `/src/pages` - Страницы (Landing, Admin)
- `/src/context` - Data Context
- `/src/data` - Демо-данные
- `/src/types` - TypeScript типы

## 🔧 Технологии

- React 19 + TypeScript
- Vite
- Tailwind CSS
- React Router
- Lucide React (иконки)

## GitHub Pages - чеклист, если "не работает"

### 1) Важно понять: GitHub Pages не запускает TypeScript/React напрямую
В репозитории лежит исходный код. Для публикации нужен build, который собирает статические файлы в папку `dist`.

### 2) В проекте уже есть GitHub Actions деплой
Файл: `.github/workflows/deploy.yml`.

### 3) Самая частая причина 404 на CSS/JS
Имя репозитория должно совпадать с `base` в `vite.config.ts`.
Сейчас стоит `base: '/phuket-dach-landing/'`.

- Если репозиторий называется `phuket-dach-landing` - ничего менять не нужно.
- Если репозиторий называется иначе - замените `'/phuket-dach-landing/'` на `'/<ваше-имя-репозитория>/'`.

### 4) Настройка Pages
GitHub - Repo - Settings - Pages:
- Source: `GitHub Actions`

### 5) SPA-роуты (/admin)
Для прямого открытия `/admin` добавлен шаг, который копирует `dist/index.html` в `dist/404.html`.
