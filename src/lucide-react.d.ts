// Temporary module declaration to satisfy TypeScript in strict mode.
// lucide-react ships its own types in recent versions; if your lockfile pulls
// an older build without typings, this prevents CI build failures.
declare module 'lucide-react';
